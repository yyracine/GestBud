import 'package:flutter/material.dart';

abstract final class AppColors {
  // Backgrounds (élévation)
  static const bg = Color(0xFF0D0F1E);
  static const surface = Color(0xFF181B33);
  static const surfaceRaised = Color(0xFF1E2240);

  // Accent & actions
  static const accent = Color(0xFF6B5CFF);
  static const accentGradient = Color(0xFF4A3FD4);
  static const accentDim = Color(0xFF2A2460);

  // Sémantique
  static const success = Color(0xFF00C897);
  static const danger = Color(0xFFFF6B6B);
  static const warning = Color(0xFFF5A623);

  // Texte
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFA8A8C0);
  static const textDisabled = Color(0xFF5A5A7A);

  // Bordures
  static const border = Color(0xFF2A2D4A);
}
